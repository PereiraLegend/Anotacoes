// Se isso ocorrer, faça isso, senão faça isso {código}
const numero = 2;
if (numero >= 0 && numero <=  3) {
    console.log("Sim o número é maior ou igual a zero");
} else {
    console.log("O Número está entre zero e três");
}
// uma vez que a engine do JS na estrutura condicional encontre um verdadeiro, mesmo sendo verdadeiro, ela vai sair da estrutura, mesmo não fazendo sentido, ou tendo nada a ver com o JS
// para cada caso é um caso, procure separar as estruturas ifs conforme o que for requerido por ua variavel ou função
